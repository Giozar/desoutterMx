---
const errors = {
    issue_question: '', 
    email_question: '', 
    phone_question: '',
    name_question: '',
    company_question: '',
    message_question: '',
}

const desoutterAPI = import.meta.env.PUBLIC_DESOUTTER_API || "https://7yc636gsjj.execute-api.us-east-1.amazonaws.com/prod";

if (Astro.request.method === 'POST') {
  try {
    const data = await Astro.request.formData()

    function getData(){

        if (data.has('email_question')) {
            data.set("subject_question","Servicio");
            return {
                subject_question: data.get('subject_question'),
                email_question : data.get('email_question'),
                phone_question : data.get('phone_question'),
                issue_question: data.get('issue_question'),
                name_question : data.get('name_question'),
                company_question : data.get('company_question'),
                message_question : data.get('message_question')
            }
        }
        
    }

    const dataContact = getData();

    if (typeof dataContact?.name_question !== 'string' || dataContact?.name_question.length < 1) {
      errors.name_question += 'Ingresa tu nombre completo. '
    }

    console.log(dataContact);

    // fetch(desoutterAPI + "contacto/servicios", {
    // method: 'POST',
    // headers: {
    //     'Content-Type': 'application/json',
    // },
    // body: JSON.stringify(dataContact),
    // })
    // .then(response => {
    //     if (!response.ok) {
    //         throw new Error(`HTTP error! Status: ${response.status}`);
    //     }
    //     return response.json();
    // })
    // .then(responseData => {
    //     if (responseData.success) {
    //         console.log('Mensaje de éxito:', responseData.message);
    //     } else {
    //         console.error('Error en la solicitud:', responseData.message);
    //     }
    // })
    // .catch(error => {
    //     console.error('Error en la solicitud:', error);
    // });


  } catch (error) {
    if (error instanceof Error) {
      console.error(error.message)
    }
  }
}
---

<div class="relative bg-white shadow dark:bg-gray-700">
    <div class="px-6 py-6 lg:px-8">
        <h3 class="mb-4 text-xl font-medium text-gray-900 dark:text-white">Escribe tu pregunta</h3>
        <form method="post">

            <div class="relative z-0 w-full mb-6 group">
                <label for="issue_question" class="block mb-2 text-sm font-normal text-gray-500 dark:text-white">Selecciona una opción</label>
                <select name="issue_question" id="issue_question" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-red-500 focus:border-red-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-red-500 dark:focus:border-red-500">
                    <option selected>Selecciona tu solicitud</option>
                    <option value="solicitud">Solicitar Servicio</option>
                    <option value="pregunta">Pregunta</option>    
                </select>
                {errors.issue_question && <p class='text-red-500'>{errors.issue_question}</p>}
            </div>

            <div class="relative z-0 w-full mb-6 group">
                <input type="email" name="email_question" id="email_question" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-white dark:border-gray-600 dark:focus:border-red-500 focus:outline-none focus:ring-0 focus:border-red-600 peer" placeholder=" " required />
                <label for="email_question" class="peer-focus:font-medium absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:left-0 peer-focus:text-red-600 peer-focus:dark:text-red-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6">Dirección de correo electrónico</label>
                {errors.email_question && <p class='text-red-500'>{errors.email_question}</p>}
            </div>

            <fieldset class="grid md:grid-cols-2 md:gap-6">
                <div class="relative z-0 w-full mb-6 group">
                    <input type="text" name="name_question" id="name_question" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-white dark:border-gray-600 dark:focus:border-red-500 focus:outline-none focus:ring-0 focus:border-red-600 peer" placeholder=" " required />
                    <label for="name_question" class="peer-focus:font-medium absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:left-0 peer-focus:text-red-600 peer-focus:dark:text-red-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6">Nombre completo</label>
                    {errors.name_question && <p class='text-red-500'>{errors.name_question}</p>}
                </div>
            </fieldset>

            <fieldset class="grid md:grid-cols-2 md:gap-6">
                <div class="relative z-0 w-full mb-6 group">
                    <input type="tel" pattern="[0-9]{3}-[0-9]{3}-[0-9]{4}" name="phone_question" id="phone_question" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-white dark:border-gray-600 dark:focus:border-red-500 focus:outline-none focus:ring-0 focus:border-red-600 peer" placeholder="ej. 123 456 7890" required />
                    <label for="phone_question" class="peer-focus:font-medium absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-[0.5] scale-75 -top-3 -z-10 origin-[0] peer-focus:left-0 peer-focus:text-red-600 peer-focus:dark:text-red-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-0">Número telefónico</label>
                    {errors.phone_question && <p class='text-red-500'>{errors.phone_question}</p>}
                </div>
                <div class="relative z-0 w-full mb-6 group">
                    <input type="text" name="company_question" id="company_question" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-white dark:border-gray-600 dark:focus:border-red-500 focus:outline-none focus:ring-0 focus:border-red-600 peer" placeholder=" " required />
                    <label for="company_question" class="peer-focus:font-medium absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:left-0 peer-focus:text-red-600 peer-focus:dark:text-red-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6">Empresa</label>
                    {errors.company_question && <p class='text-red-500'>{errors.company_question}</p>}
                </div>
            </fieldset>

            <fieldset>
                <div class="relative z-0 w-full mb-6 group">
                    <textarea name="message_question" id="message_question" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-white dark:border-gray-600 dark:focus:border-red-500 focus:outline-none focus:ring-0 focus:border-red-600 peer" placeholder=" " required cols="10" rows="1"></textarea>
                    <label for="message_question" class="peer-focus:font-medium absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:left-0 peer-focus:text-red-600 peer-focus:dark:text-red-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6">Pregunta</label>
                    {errors.message_question && <p class='text-red-500'>{errors.message_question}</p>}
                </div>
            </fieldset>

            <button type="submit" class="text-white bg-[#E40520] hover:bg-red-800 focus:ring-4 focus:outline-none focus:ring-red-300 font-medium rounded-lg text-sm w-full sm:w-auto px-5 py-2.5 text-center dark:bg-red-600 dark:hover:bg-[#E40520] dark:focus:ring-red-800">Enviar</button>
        </form>    
    </div>
</div>

<script>
    function init () {
        const btnQuestion = document.querySelector('#btn-question');
        const formQuestion = document.querySelector('#form-question');
        const btnCloseQuestion = document.querySelector('#btn-close-question');

        btnQuestion?.addEventListener('click', ()=>{
            formQuestion?.classList.replace('hidden', 'flex');
        });

        btnCloseQuestion?.addEventListener('click', ()=>{
            formQuestion?.classList.replace('flex','hidden');
        })

        document.getElementById('phone_question')?.addEventListener('input', function (e) {
        // Eliminar cualquier caracter que no sea un número
        const phoneNumber = e.target.value.replace(/\D/g, '').slice(0, 10);

        // Aplicar el formato
        let formattedPhoneNumber = '';
        for (let i = 0; i < phoneNumber.length; i++) {
                if (i === 3 || i === 6) {
                    formattedPhoneNumber += '-';
                }
                formattedPhoneNumber += phoneNumber[i];
            }

            // Actualizar el valor del campo de entrada
            e.target.value = formattedPhoneNumber;
        });
    }

    window.onload = init;
    document.addEventListener('DOMContentLoaded', init);
    init();
    document.addEventListener('astro:page-load', init);
    
  </script>