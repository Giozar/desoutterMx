---
const { subsubcategory } = Astro.props;
const {categoryId, categoryName, subcategoryId, subcategoryName} = Astro.params;
---

<section class="group relative overflow-hidden bg-white w-11/12 h-64">
  <figure class="z-0">
    <div class="flex justify-center">
      <picture>
        <img src={subsubcategory.image} title={"Herramienta industrial " + subsubcategory.name} alt={" Herramienta industrial " + subsubcategory.name + " para " + categoryName } loading="lazy" class="w-52 h-52">
      </picture>
    </div>
    <a href=`/herramientas/${categoryId}/${categoryName}/${subcategoryId}/${subcategoryName}/${subsubcategory.id}/${subsubcategory.pathName}` title={subsubcategory.pathName}>
      <figcaption
        class="absolute top-0 z-20 grid h-full w-full translate-y-48 gap-y-2 border-t border-red-600 bg-white bg-opacity-90 transition-transform duration-300 group-hover:-translate-y-0 group-hover:opacity-100"
      >
        <header class="px-5">
          <h3 class="text-center font-bold text-gray-700">{subsubcategory.name}</h3>
        </header>
        <p class="p-2 pt-10 text-center text-sm font-semibold text-gray-600">
          {subsubcategory.description}
        </p>
        <legend
          class="mb-2 h-8 justify-self-center bg-red-600 px-6 py-1 text-center text-sm font-semibold text-white"
          >Mas informacion</legend
        >
      </figcaption>
    </a>
  </figure>
</section>
